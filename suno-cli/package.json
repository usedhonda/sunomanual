{
  "name": "@usedhonda/suno-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Suno retrieval and dry-run create CLI.",
  "bin": {
    "suno-cli": "./dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test \"dist/test/**/*.test.js\""
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^24.0.10",
    "typescript": "^5.8.3"
  }
}
